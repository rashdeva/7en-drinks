# Skip optional dependencies to reduce build failures
optional=false

# Avoid building native modules if possible
node-gyp-force-python=python3
build-from-source=false

# Increase network timeout for slow connections
network-timeout=100000

# Use legacy peer deps to avoid dependency conflicts
legacy-peer-deps=true
